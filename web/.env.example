# Frontend Environment Variables

# API Configuration
# If not set, the app will use relative paths (/api/v1) which work with the Vite proxy
# Use VITE_API_BASE for general API calls and VITE_API_BASE_URL for specific services if needed.
VITE_API_BASE=
VITE_API_BASE_URL=

# Development Server Configuration
VITE_DEV_SERVER_PORT=5173

# Proxy Target for Development (used in vite.config.js)
# This is where your backend API is running during development
VITE_PROXY_TARGET=http://localhost:8080

# Debug Mode
VITE_DEBUG_MODE=true

# Feature Flags
VITE_ENABLE_AUTH=false
