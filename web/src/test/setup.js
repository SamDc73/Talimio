// Global Vitest setup, wired up by `setupFiles` in vitest.config.js.
// Registers the @testing-library/jest-dom matchers (toBeInTheDocument,
// toHaveAttribute, toBeEmptyDOMElement, ...) that the component tests assert with.
import "@testing-library/jest-dom/vitest"
