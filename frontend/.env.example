# Frontend Environment Variables

# API Configuration (Optional)
# Only set this if your API is on a different domain/port than your frontend
# If not set, the app will use relative paths (/api/v1) which work with the Vite proxy
# Examples:
#   - Same domain (default): Leave unset or use relative path
#   - Different port: VITE_API_BASE=http://localhost:8080/api/v1
#   - Different domain: VITE_API_BASE=https://api.example.com/api/v1
# VITE_API_BASE=

# Development Server Configuration
VITE_DEV_SERVER_PORT=5173

# Proxy Target for Development (used in vite.config.js)
# This is where your backend API is running during development
VITE_PROXY_TARGET=http://localhost:8080

# Debug Mode
VITE_DEBUG_MODE=true

# Feature Flags
VITE_ENABLE_AUTH=true
VITE_ENABLE_PERSONALIZATION=true
VITE_ENABLE_ANALYTICS=false

# Optional: External Services
# VITE_SENTRY_DSN=
# VITE_GA_TRACKING_ID=
